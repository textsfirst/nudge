# Secrets only — all other settings live in nudge.config.yaml.

# Photon / Spectrum Cloud credentials from https://app.photon.codes
SPECTRUM_PROJECT_ID=
SPECTRUM_PROJECT_SECRET=
SPECTRUM_WEBHOOK_SECRET=

# Optional API provider key (see provider.openai in nudge.config.yaml).
OPENAI_API_KEY=

# Optional: enables the web_search and web_extract tools (https://firecrawl.dev).
FIRECRAWL_API_KEY=
